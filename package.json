{
  "name": "party-level-architect",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name party-level-architect --branch main --commit-dirty=true"
  },
  "dependencies": {
    "lil-gui": "^0.19.2",
    "phaser": "^3.85.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "wrangler": "^4.100.0"
  }
}
